[project]
name = "chapter06-agent-testing"
version = "0.1.0"
description = "第6章: エージェントのテスト（穴埋めハンズオン）"
requires-python = ">=3.12"
dependencies = ["httpx", "strands-agents"]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["verify"]
