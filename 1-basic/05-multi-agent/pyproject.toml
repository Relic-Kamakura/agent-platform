[project]
name = "chapter05-multi-agent"
version = "0.1.0"
description = "第5章: マルチエージェント（穴埋めハンズオン）"
requires-python = ">=3.12"
dependencies = ["strands-agents", "botocore[crt]"]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["verify"]
