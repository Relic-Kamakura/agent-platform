[project]
name = "chapter15-mcp"
version = "0.1.0"
description = "第15章: MCP サーバ（写経ハンズオン）"
requires-python = ">=3.12"
dependencies = ["strands-agents", "mcp"]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["verify"]
pythonpath = ["."]
