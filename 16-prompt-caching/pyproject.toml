[project]
name = "chapter16-prompt-caching"
version = "0.1.0"
description = "第16章: プロンプトキャッシュ（穴埋めハンズオン）"
requires-python = ">=3.12"
dependencies = ["strands-agents", "botocore[crt]"]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["verify"]
