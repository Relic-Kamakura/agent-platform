[project]
name = "chapter18-hitl"
version = "0.1.0"
description = "第18章: HITL・承認ゲート（穴埋めハンズオン）"
requires-python = ">=3.12"
dependencies = ["strands-agents", "botocore[crt]"]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["verify"]
