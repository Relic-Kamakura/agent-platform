[project]
name = "chapter10-knowledge-base"
version = "0.1.0"
description = "第10章: ナレッジベース — RAG の仕組みを手で作る（写経ハンズオン）"
requires-python = ">=3.12"
dependencies = ["boto3"]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["verify"]
pythonpath = ["."]
