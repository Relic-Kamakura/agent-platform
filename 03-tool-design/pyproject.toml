[project]
name = "chapter03-tool-design"
version = "0.1.0"
description = "第3章: ツール設計（穴埋めハンズオン）"
requires-python = ">=3.12"
dependencies = ["httpx", "strands-agents", "botocore[crt]"]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["verify"]
