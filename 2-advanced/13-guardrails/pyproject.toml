[project]
name = "chapter17-guardrails"
version = "0.1.0"
description = "第17章: Bedrock Guardrails（穴埋めハンズオン）"
requires-python = ">=3.12"
dependencies = ["strands-agents", "botocore[crt]"]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["verify"]
